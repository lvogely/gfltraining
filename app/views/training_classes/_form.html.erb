<%= form_for(@training_class) do |f| %>
  <% if @training_class.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@training_class.errors.count, "error") %> prohibited this training_class from being saved:</h2>

      <ul>
      <% @training_class.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
